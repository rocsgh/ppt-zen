<p align="center"><a href="DESIGN.md">English</a> · <b>简体中文</b></p>

# 设计与架构

由来、推导、以及被我们推翻过的模型。这是抄不走的那部分。

---

## 由来

这套东西不是纸上推的，是**一次次一夜做出 70 页整图 deck 逼出来的**。下面每条规则背后都有一次踩坑。规则和实践打架时，实践赢——包括我们自己早期的模型（见文末"被推翻的"）。

## 第零步：一根旋钮定详略

在一切之前，每页先做一个判断：

> **这页是"一句话说得完"（→ 提纲挈领），还是"必须并置才成立"（→ 细化）？**

因为**听觉是线性的，视觉是并置的。** 提纲挈领页归"演说之禅"（留白、克制），细化页归"信息设计"（密度）。两条自动规则：证据页必须细化（证据看不见就不成立）；细化页后面接一页疏的。

## 四根轴

**详略 × 骨架 × 器物 × 材质。** 相互独立，别混。

- **详略**——提纲挈领 ↔ 细化（第零步）。逐页，AI 定。
- **骨架**——画面怎么被切分（网格/光带/流线/色域/对峙…可不派）。按页面职能自动派，不问用户。
- **器物——这一页的论点，画成什么东西。** 这是所有人都漏的一根。材质管"用什么做的"，骨架管"怎么切"，都不管"画什么"。那个空档就是器物：尺度→一把尺；"问一次纯属运气"→点阵散开又收敛成一条线；"你的客户在见你之前"→一只举起的手。两道闸：紧扣主题（不是"跟主题沾边的好看东西"）+ 抓住注意（不是角落里的装饰）。自查：看着它能不能猜出这页在说什么？
- **材质**——用什么做的（水墨/铜版/铅笔…）。全片选一次；唯一用户可能要表态的一根。

### 插画是量级，不是有/无（L0–L4）
L0 真无 · L1 边饰研究稿（只负责材质可信度） · L2 排印即插画 · L3 点缀（替代一句话的小图） · L4 主插画（论点就是它）。好页通常是叠出来的（大字 L2 + 蓝叉 L3 + 四角草图 L1）。

### 跨页一致性闸
前三道闸都是单页的，第四道不是：反复出现的角色/概念，身份约定要写进**每一页**的 prompt（例："Maya = 你雇的一个人，永不画成机器人/齿轮/电路"）。模型不知道别的页画了什么，材质会把它带回自己的老路，不明写就跑偏。靠**并排看图**查，读 prompt 看不出来。

## 出图 = 三轴相乘
```
SURFACE:  材质配方（所有笔触与介质）
SKELETON: 纯几何构图（版式；不出现材质词；可"素面"）
DEVICE:   这页的器物（留槽，逐页填）
END:      满幅，用 SURFACE 材质、按 SKELETON 构图，画出 DEVICE
```
只写 SURFACE + SKELETON 会把 deck 做薄。DEVICE 这一槽最值钱、也最不能系统化。

### 四条出图硬规则（踩坑换来的，不是理论）
1. **密度分层**——背景占满（压淡）、唯一主体约 70% 且密度长在内部、前景限量；禁浮动数据卡。
2. **禁自创中文**——装饰记号只许几何/数字/尺寸线。
3. **结构词抑制**——结尾 CRITICAL 段逐词列禁画词（STYLE / LAYOUT / VERBATIM / caption / eyebrow…）。
4. **中文标题会多吐一个字**——出图后逐字校对标题。

## 三层选型：介质 → 手笔 → 世界
- **介质**——这页是什么做的（羊皮纸/胶片/铜版）。材质轴的根。
- **手笔**——这个介质里是谁的手：一个流派 = 调色 + 布光 + 构图习惯 + 场景母题 + 情绪的成套 DNA 包（会伸进骨架轴）。诺兰和维伦纽瓦是同一介质、两只手。
- **世界**——观众进入什么设定（自带信息语法/镜头/时间/隐藏符号）。选材质的真判据是*世界观设定*，不是单纯华丽。

顺序：定世界 → 选介质 → 有子库再选手笔 → 骨架按职能自动派 → 器物逐页想。

## 流派体系（组织法，开源）
**族**是介质的世界；**流派**是打过整仗、四轴齐、可复产、可命名的成套包；**候选核**是有锚未实战的半成品；**散件**是无锚纯技法。做新 deck：命中流派→直接编译；不命中→从候选核升格；单页特殊→散件特调。两条铁则：**换材质必须换骨架**（只换皮＝全片一个模子）；**世界重构工序** = 一个规格表（每页：场景/海域 + 骨架 + 器物）驱动全部 prompt，世界统一且根治重复。

⚠️ *方法*开源。具体的材质母版 / 流派 / 大师手笔是**社区共建的库**（`styles/`）——自备。

## 被推翻的模型（留作警告）
- "两种产物（演讲背景图 / 信息 deck）"二分法 → 被一根逐页旋钮取代（那是我们自己的旧模型，实践中被推翻）。
- 插画二值（有/无）→ 被 L0–L4 量级取代（当成开关，章节页和快切页就出得光秃秃）。
- "多问用户显得友好"——每多问用户一个问题，就是这套判断少了一条。
